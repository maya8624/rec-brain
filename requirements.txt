# ============================================================
# Python 3.11 compatible — merged & pinned
# Last verified: March 2026
# ============================================================

# ------------------------------------
# Web Framework
# ------------------------------------
fastapi==0.115.12           # upgraded: 0.112.2 → 0.115.12 (bug fixes, Python 3.11 improvements)
uvicorn[standard]==0.34.0   # upgraded: 0.30.6 → 0.34.0 (added [standard] for websocket + reload support)
python-dotenv==1.0.1        

# ------------------------------------
# Validation & Config
# ------------------------------------
pydantic==2.12.5            # upgraded: 2.11.3 → 2.12.5 (required by llama-index-core 0.14.x)
pydantic[email]==2.12.5
pydantic-settings==2.9.1    # upgraded: 2.4.0 → 2.9.1

# ------------------------------------
# AI & LLM — upgraded to 0.3.x (breaking change from your 0.2.x)
# NOTE: all langchain-* packages must be upgraded together
# ------------------------------------
langchain==0.3.25           # upgraded: 0.2.14 → 0.3.25
langchain-community==0.3.24 # upgraded: 0.2.12 → 0.3.24
langchain-core==0.3.59      # fixed: 0.3.59 required by langchain-community 0.3.24
langchain-ollama==0.3.2     # upgraded: 0.1.3 → 0.3.2 (must match langchain 0.3.x)
langchain-groq==0.3.2       # upgraded: 1.1.2 → 0.3.2 (re-versioned to align with langchain 0.3.x)
langgraph==0.3.5            
langgraph-checkpoint-postgres==0.3.5  

# ------------------------------------
# Vector DB
# ------------------------------------
langchain-chroma==0.2.2     # upgraded: 0.1.3 → 0.2.2 (compatible with langchain 0.3.x)
chromadb==0.6.3             # upgraded: 0.5.23 → 0.6.3 (supports tokenizers>=0.20.3)
langchain-huggingface==0.1.2  
sentence-transformers==3.3.1  

# ------------------------------------
# Database
# ------------------------------------
sqlalchemy==2.0.32
psycopg2-binary==2.9.9
tabulate==0.9.0

# ------------------------------------
# LlamaIndex — RAG (vector search via pgvector)
# ------------------------------------
llama-index-core==0.14.19
llama-index-vector-stores-postgres==0.8.1
llama-index-embeddings-huggingface==0.7.0   # query embedding: sentence-transformers/all-MiniLM-L6-v2
pgvector==0.3.6
asyncpg==0.30.0

# ------------------------------------
# HTTP & Resilience
# ------------------------------------
httpx==0.28.1               # upgraded: 0.27.0 → 0.28.1 (async improvements)
tenacity==9.0.0            

# ------------------------------------
# Logging
# ------------------------------------
structlog==25.1.0           

# ------------------------------------
# Ollama
# ------------------------------------
ollama==0.4.8               

# ------------------------------------
# Testing
# ------------------------------------
pytest==8.3.5               
pytest-asyncio==0.26.0      
pytest-httpx==0.35.0        

redis>=5.2,<6.0